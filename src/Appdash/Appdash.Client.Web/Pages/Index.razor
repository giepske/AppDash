@page "/"

    <Grid>
        @foreach (ComponentBase component in _components)
        {
            <GridItem>
                @RenderComponent(component)
            </GridItem>
            <GridItem>
                @RenderComponent(component)
            </GridItem>
            <GridItem>
                @RenderComponent(component)
            </GridItem>
            <GridItem>
                @RenderComponent(component)
            </GridItem>
            <GridItem>
                @RenderComponent(component)
            </GridItem>
            <GridItem>
                @RenderComponent(component)
            </GridItem>
        }
    </Grid>